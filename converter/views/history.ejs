<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="stylesheet" href="styles.css" />
		<title>HISTORY</title>
	</head>
	<body>
		<div class="history">
			<h1>Conversion History</h1>
			<div class="history_content">
				<ul>
					<% history.forEach(item =>{%> <% if(item.conversionType &&
					item.numberInput !== undefined && item.conversion !== undefined &&
					item.timeStamp !== undefined){%>
					<li>
						<p><b>conversion Type: </b><%= item.conversionType %></p>
						<p><b>numberInput: </b><%= item.numberInput %></p>
						<p><b>conversion result: </b><%= item.conversion %></p>
						<p><b>Time Stamp: </b><%= item.timeStamp %></p>
					</li>
					<% } %> <% }) %>
				</ul>
			</div>
		</div>
	</body>
</html>
